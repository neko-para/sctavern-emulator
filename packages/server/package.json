{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild --platform=node --bundle index.ts --outfile=index.js",
    "run": "npm run build && node index.js"
  },
  "dependencies": {
    "@sctavern-emulator/framework": "*",
    "@sctavern-emulator/emulator": "*",
    "ws": "^8.11.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.3",
    "esbuild": "^0.16.12",
    "typescript": "^4.9.3"
  }
}
